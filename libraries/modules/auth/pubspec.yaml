name: auth
description: A new Flutter package project.
version: 0.0.1

publish_to: "none"

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dependency_manager:
    path: ../../dependency_manager
  ds:
    path: ../../ds

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/Backgrounds/
